# SFT chosen 生成目标

输入：

```text
用户画像 + TripRequest + 高德地图真实工具结果快照
```

输出：

```text
严格符合 TripPlan schema 的 JSON
```

主要约束：

- 景点和酒店优先从高德工具结果中选择。
- 不编造工具结果中不存在的 POI。
- 每天 2-3 个景点。
- 每天包含早餐、午餐、晚餐。
- 预算、节奏、饮食禁忌、同行人和避雷项必须体现在方案中。
- 只输出 JSON，不输出解释文本。
