# 实验记录卡：260521 continue from merged ckpt66

## 目标

`260520 closing budget clean` 第一炉单生成没有涨，直觉上可能有两个解释：一是数据方向没有直接转化成单生成收益，二是学习率太小或训练不够，仍然欠拟合。

这一炉从 `merged ckpt66` 继续训练，把 learning rate 从 `1e-6` 提到 `1.5e-6`，用同一批 closing 数据验证第二种可能。

## 数据

继续使用 `260520_closing_budget_clean`：

- 训练集：`trip_planner_dpo_260520_closing_budget_clean_train`
- pair 总量：`1115`
- train pairs：`1026`
- val pairs：`89`
- frozen eval signature overlap：`0`

## 训练参数

配置：`training/configs/qwen25_7b/dpo_260521_closing_budget_clean_from_merged_ckpt66_r32_2epoch_lr1p5e6_ctx24576_z3nooffload_label_logits_4gpu0123.yaml`

| 参数 | 值 |
| --- | --- |
| 起点 | `merged_dpo_260521_closing_ckpt66_for_continue_dpo` |
| dataset | `trip_planner_dpo_260520_closing_budget_clean_train` |
| lora rank | 32 |
| learning rate | 1.5e-6 |
| epoch | 2 |
| cutoff_len | 24576 |
| GPU | 0,1,2,3 |
| save_steps | 16 |

训练完成 `66` step。loss 从 `0.38` 左右下降到 `0.16` 到 `0.24`，accuracy 后半程基本为 `1.0`。从训练日志看，模型确实学到了这批 preference，但是否转化为评测收益还要看 planner soft。

## 评估结果

评估报告：`training/outputs/eval/by_model/dpo_260521_closing_budget_clean_from_merged_ckpt66_ckpt_model_parallel_23_w10/dpo_260521_closing_budget_clean_from_merged_ckpt66_checkpoint-64_report/dpo_260521_closing_budget_clean_from_merged_ckpt66_checkpoint-64_full_report.md`

| 指标 | 260520 ckpt66 | 260521 ckpt64 | 变化 |
| --- | ---: | ---: | ---: |
| hard pass | 99.0% | 98.2% | -0.8pp |
| planner soft | 70.1% | 69.7% | -0.4pp |
| recomputed budget soft | 48.3% | 47.6% | -0.7pp |
| budget preference aligned | 81.4% | 81.7% | +0.3pp |
| recomputed budget fit | 56.5% | 56.6% | +0.1pp |

## 结论

继续训练没有改善单生成。到这里如果只看单生成，应该回到 `260519 checkpoint-138`。

不过这炉仍然有价值：它改变了候选分布。后续多温度 rerank 证明，`ckpt64` 虽然单生成不强，但在候选池里能提供更好的可选答案。
