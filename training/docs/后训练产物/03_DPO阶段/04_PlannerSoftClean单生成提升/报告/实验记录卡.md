# 实验记录卡：260519 ps2400clean plus direct402

## 目标

上一轮 `260518 core1418_plus_direct402 checkpoint-126` 已经证明 direct402 能稳住格式和 hard pass，但 planner soft 仍有明显空间。这一炉的目标是用更完整的 `planner_soft2400_clean` 放大有效偏好信号，并混入 `fd600_direct402` 作为 anchor，避免前一轮有效能力被冲掉。

## 数据

数据摘要：`training/data/planner/dpo/260519_planner_soft2400_clean_plus_direct402/summary.json`

| split | 总量 | planner_soft2400_clean | fd600_direct402 |
| --- | ---: | ---: | ---: |
| train | 2164 | 1802 | 362 |
| val | 240 | 200 | 40 |

数据策略：

- 主信号来自完整 planner soft clean 数据。
- `fd600_direct402` 用作 anchor，保留前一轮已验证的 direct preference。
- 按 prompt hash 去重，val 排除 train prompt。

LLaMA-Factory 数据：

- `training/data/llamafactory/generated/trip_planner_dpo_260519_planner_soft2400_clean_plus_direct402_train.json`
- `training/data/llamafactory/generated/trip_planner_dpo_260519_planner_soft2400_clean_plus_direct402_val.json`

## 训练参数

配置：`training/configs/qwen25_7b/dpo_260519_ps2400clean_plus_direct402_from_ckpt126_r32_3epoch_ctx24576_z3nooffload_label_logits_6gpu012345.yaml`

| 参数 | 值 |
| --- | --- |
| 起点 | `merged_dpo_260519_core1418_plus_direct402_ckpt126_for_next_dpo` |
| lora rank | 32 |
| learning rate | 2e-6 |
| epoch | 3 |
| cutoff_len | 24576 |
| GPU | 0,1,2,3,4,5 |
| save_steps | 23 |
| deepspeed | ZeRO-3 no offload |
| 显存优化 | `LLAMAFACTORY_DPO_LABEL_LOGITS_ONLY=1` |

训练完成 `138` step，耗时约 `16.5` 小时。日志尾部 loss 多数在 `0.01` 到 `0.15`，reward accuracy 多数在 `0.96` 到 `1.0`，说明这批 pair 的偏好区分比较稳定。

## 评估结果

评估报告：`training/outputs/eval/by_model/dpo_260519_ps2400clean_plus_direct402_from_ckpt126_ckpt_model_parallel_012345_w10/dpo_260519_ps2400clean_plus_direct402_from_ckpt126_checkpoint-138_report/dpo_260519_ps2400clean_plus_direct402_from_ckpt126_checkpoint-138_full_report.md`

| 指标 | ckpt126 baseline | 260519 ckpt138 | 变化 |
| --- | ---: | ---: | ---: |
| hard pass | 98.4% | 98.4% | +0.0pp |
| planner soft | 66.9% | 71.5% | +4.6pp |
| recomputed budget soft | 48.5% | 50.9% | +2.4pp |
| budget preference aligned | 79.2% | 82.4% | +3.2pp |
| recomputed budget fit | 57.3% | 58.7% | +1.4pp |

## 结论

这是单生成主线里最关键的一跳。hard pass 没被牺牲，planner soft 提升 `4.6pp`，预算偏好贴合也提升。后续所有 closing 实验都应该把这轮的 `checkpoint-138` 当作单生成强基线。
